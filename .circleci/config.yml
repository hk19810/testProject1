version: 2.1

orbs:
  maven-deps: moole/dependency-extractor (1.0.0) # Replace with your actual Orb namespace and version

workflows:
  version: 2
  dependency-check:
    jobs:
      - maven-deps/dependency-extraction
